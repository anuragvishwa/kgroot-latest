# AI SRE Platform Dependencies

# Core - Updated versions to resolve anyio conflict
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-dotenv==1.0.0

# Neo4j
neo4j==5.14.0

# OpenAI (for GPT-5)
openai>=1.55.0

# Claude Agent SDK (for incident resolution with real tool execution)
claude-agent-sdk>=0.1.0

# AWS SDK (for Bedrock integration)
boto3>=1.34.0

# Async - Compatible with claude-agent-sdk
anyio>=4.0.0
aiohttp==3.9.1

# YAML parsing (for rollback manager)
pyyaml==6.0.1

# Logging
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.23.0
pytest-cov==4.1.0
httpx==0.27.0

# Development
black==23.11.0
ruff==0.1.6
mypy==1.7.1

# Monitoring (optional)
prometheus-client==0.19.0
